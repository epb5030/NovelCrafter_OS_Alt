version: '3.8'

services:
  opencrafter:
    build:
      context: .
      dockerfile: Dockerfile
    image: opencrafter:latest
    container_name: opencrafter
    restart: unless-stopped
    ports:
      - "3005:3005"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3005
      - DATABASE_DIR=/app/data
      - NODE_ENV=production
